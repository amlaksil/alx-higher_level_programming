window.onload = function () {
  /* Is vanilla JavaScript function equivalent to '.(document).ready()' function in JQuery. Executes when the DOM(Document Object Model) is ready. */
  document.querySelector('header').style.color = '#FF0000';
};
