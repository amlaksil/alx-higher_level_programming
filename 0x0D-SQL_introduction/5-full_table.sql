-- prints the full description a table `first_table` from the
-- database `hbtn_0c_0` in your MySQL server
-- the first nonoption argument is taken as the name of default database
SHOW CREATE TABLE first_table;
