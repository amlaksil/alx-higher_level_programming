-- removes all records with a `score <= 5` in the table `second_table` of the
-- database `hbtn_0c_0`
-- the first nonoption argument is taken as the name of default database
DELETE FROM second_table WHERE score <= 5;
